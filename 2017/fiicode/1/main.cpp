#include <iostream>
#include <algorithm>

using namespace std;

int main()
{
    cin >> m;
    for (i=1; i<=m; i++)
    {
        cin >> e[i] >> v[i];
    }
    cin >> n;
    for (i=1; i<=n; i++)
        cin >> x[i];
}
