#include <fstream>
#include <algorithm>
#include <set>

using namespace std;

ifstream fin("pikachu.in");
ofstream fout("pikachu.out");

int main()
{
    fin >> n >> k;
    for (i=1; i<=n; i++)
        fin >> a[i];
}
